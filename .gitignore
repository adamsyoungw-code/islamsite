# Telegram-сессии агента (доступ к аккаунту — не коммитить)
*.session
*.session-journal

# Python
__pycache__/
*.pyc

# Черновые транскрипции из контент-пайплайна (промежуточный артефакт)
tools/transcripts/
